## طرح جامع پنل مدیریت ربات بازی تلگرام

این سند، مشخصات کامل و عملیاتی یک پنل مدیریت تحت وب برای ربات را تعریف می‌کند. پنل روی همان پایگاه داده مشترک با ربات اجرا می‌شود و تمام قابلیت‌های درخواستی کارفرما را با امنیت و کارایی مناسب پوشش می‌دهد.

### ۱) معماری، امنیت و احراز هویت
- **زبان/فریم‌ورک**: PHP 8.1+، PDO، معماری MVC سبک (یا Micro MVC) بدون وابستگی الزامی به فریم‌ورک. رابط کاربری با Bootstrap 5 و یک تم RTL.
- **اتصال پایگاه‌داده**: PDO با DSN مشابه ربات، Pool محدود، خط‌مشی‌ استفاده از تراکنش‌ها برای عملیات بحرانی (تغییر امتیاز، تایید انتقال و ...).
- **احراز هویت مدیر**:
  - ورود مبتنی بر نشست (Session) + کوکی امن (HttpOnly, SameSite=Lax/Strict, Secure در HTTPS).
  - اعتبارسنجی دو مرحله‌ای اختیاری: تایید کد یک‌بار مصرف (OTP) ارسالی در تلگرام به `ADMIN_ID` یا TOTP.
  - RBAC ساده: نقش‌ها (owner, admin, moderator, auditor) با ماتریس دسترسی به منوها/اعمال.
  - محافظت CSRF روی تمام فرم‌های POST/PUT/DELETE.
- **سخت‌سازی امنیتی**:
  - محدودیت نرخ (Rate Limit) ورود و اعمال حساس؛ ثبت تلاش‌های ناموفق.
  - ضد XSS/SQLi از طریق Prepared Statement و Escaping مناسب.
  - لاگ‌گیری عملیات حساس در جدول `audit_logs` (در صورت اضافه شدن) شامل: شناسه مدیر، IP، User-Agent، زمان و جزئیات عملیات.

### ۲) ناوبری و ساختار صفحات
- **نوار بالایی**: لوگو، محیط (Production/Stage)، وضعیت ربات (فعال/تعمیر)، منوی کاربر (پروفایل/خروج).
- **منوی کناری**:
  - داشبورد
  - مدیریت کاربران
    - لیست کاربران
    - پروفایل کاربر
    - مدیریت کشورها (اختصاص/پس‌گیری)
  - مدیریت فروشگاه
    - دسته‌بندی‌ها (عادی/VIP)
    - آیتم‌ها (ایجاد/ویرایش/حذف)
  - مدیریت کارخانه‌ها
    - انواع کارخانه
  - مدیریت کشورها (ایجاد/ویرایش + نوع رایگان/VIP)
  - بازبینی ارسالی‌های کاربران (Play)
    - Sabotage Rolls
    - Security Rolls
    - Scenarios
  - مدیریت سیستم سوالات (گروه)
    - سوالات و پاسخ صحیح
    - بازه جوایز
  - تنظیمات کلی ربات
    - توکن، آیدی ادمین، آیدی گروه، حالت تعمیر، Broadcast
  - گزارشات و لاگ‌ها (تراکنش‌ها، جوایز، خریدها، انتقال‌ها)

### ۳) داشبورد
- **کارت‌های آماری**:
  - تعداد کل کاربران
  - کاربران فعال ۲۴ ساعت اخیر (بر اساس `last_active_at`)
  - مجموع درآمد تومانی (VIP خریدها) و مجموع امتیازی (Earn/Spend/Reward)
- **نمودارها**:
  - رشد کاربران در ۳۰ روز اخیر
  - تعداد ارسالی‌های کاربران در ۷ روز اخیر
- **فعالیت اخیر**: آخرین تراکنش‌ها، تایید/رد ارسالی، انتقال‌های تایید شده.

### ۴) مدیریت کاربران
- **لیست کاربران**: جدول با جستجو (آیدی عددی تلگرام، نام کاربری، نام کشور)، فیلتر وضعیت (بن‌شده/دارای کشور/بدون کشور/ادمین).
- **پروفایل کاربر**:
  - اطلاعات پایه (تلگرام، نام، کشور، امتیاز، تاریخ ایجاد/فعالیت).
  - **عملیات**: افزایش/کاهش امتیاز (با ثبت در `transactions`)، بن/رفع بن.
  - **دارایی‌ها**:
    - آیتم‌ها (`user_items`) با امکان حذف/افزودن دستی.
    - کارخانه‌ها (`user_factories`): مشاهده سطح، زمان ارتقا/سردشدن، امکان ارتقا/ریست با احتیاط و ثبت لاگ.
  - **کشور**:
    - اختصاص دستی کشور VIP به کاربر (در صورت آزاد بودن کشور).
    - پس‌گیری کشور از کاربر (تنظیم `country_id = NULL` و آزادسازی).
- **تایید انتقال آیتم**:
  - لیست `transfers` با وضعیت `pending`.
  - با تایید: انتقال `user_items` به `to_user_id` (یا ایجاد ردیف جدید برای مقصد و حذف/کاهش مبدأ بر حسب Quantity).
  - با رد: به‌روزرسانی وضعیت و ارسال پیام رد.

### ۵) مدیریت محتوای بازی
- **فروشگاه**:
  - دسته‌بندی‌ها (`shop_categories`): ایجاد/ویرایش/حذف، تعیین نوع (normal/vip) و ترتیب نمایش.
  - آیتم‌ها (`items`): نام، توضیحات، نوع VIP یا عادی، قیمت امتیازی/تومانی، نوع اعطا (`grant_type`: force/factory/points)، ارتباط با `factory_types`، فعال/غیرفعال، ترتیب.
- **کارخانه‌ها**:
  - انواع کارخانه (`factory_types`): نام، قیمت پایه، درآمد پایه، حداکثر سطح، فعال/غیرفعال، توضیحات.
  - نمایش تعداد کاربران مالک هر نوع.
- **کشورها**:
  - `countries`: ایجاد/ویرایش/حذف نرم (غیرفعال‌سازی)، تعیین نوع رایگان/VIP، وضعیت فعال.

### ۶) بازبینی ارسالی‌های کاربران (Play → Submissions)
- سه زیرمنو: Sabotage, Security, Scenarios (فیلتر `type`).
- لیست با مرتب‌سازی صعودی زمان (`pending` قدیمی‌تر در اول).
- جزئیات ارسالی: نمایش متن و عکس (تغییر اندازه مناسب)، نام کشور و زمان ارسال.
- **اعمال**:
  - ✅ تایید: تغییر `status` به `approved`, `reviewed_at`, `reviewed_by` = مدیر جاری، ارسال پیام تایید به کاربر (لینک مشاهده مجدد محتوا در پیام).
  - ❌ رد: مشابه بالا با `rejected` + پیام رد به کاربر.

### ۷) مدیریت سیستم سوالات گروهی
- **سوالات (`questions`)**: CRUD کامل، فیلد `correct_answer`، وضعیت فعال.
- **بازه جوایز**: تنظیم `settings.reward_min`, `settings.reward_max`.
- **زمان‌بندی ارسال**: به‌صورت Cron (خارج از پنل) هر ۲ ساعت؛ پنل فقط وضعیت آخرین ارسال‌ها (`question_posts`) و شمارش سوالات فعال را نشان می‌دهد.
- **نظارت**: جدول آخرین سوالات ارسال‌شده، برنده‌ها، امتیاز اعطا شده، حذف خودکار پس از ۱۰ دقیقه.

### ۸) تنظیمات کلی ربات
- **پارامترها**:
  - BOT_TOKEN، ADMIN_ID، ADMIN_USERNAME، GROUP_CHAT_ID (هشدار امنیتی: نگهداری امن و تغییر کنترل‌شده).
  - Maintenance Mode (روشن/خاموش)؛ این مقدار با `MAINTENANCE` در کد هماهنگ یا از `settings` خوانده شود (راهکار: یک لایه کانفیگ قابل Override).
- **Broadcast**:
  - فرم متن پیام (HTML مجاز)، انتخاب فیلتر دریافت‌کنندگان (همه، کاربران دارای کشور، کشور خاص، بن‌نشده‌ها، فعال ۳۰ روز اخیر).
  - صف‌بندی ارسال با نرخ محدود (مثلاً 20 msg/sec) و گزارش وضعیت/خطاها.

### ۹) گزارشات و پایش
- **تراکنش‌ها (`transactions`)**: فیلتر بر اساس کاربر، نوع، بازه زمانی؛ جمع کل‌ها.
- **خریدهای VIP (`vip_purchase_requests`)**: وضعیت جریان (requested/contacted/completed/cancelled)، لینک سریع به پروفایل کاربر.
- **درآمد کارخانه‌ها**: شمار دریافت‌های روزانه/هفتگی، درآمد تجمیعی اعطا شده.
- **لاگ ممیزی (اختیاری)**: `audit_logs` شامل عملیات مدیران (چه کسی، کجا، چه زمانی، چه تغییری).

### ۱۰) تجربه کاربری و جزئیات UI
- **RTL کامل**، فونت مناسب فارسی، دکمه‌های واضح تایید/لغو.
- **Toast/Alert استاندارد** برای بازخورد سریع.
- **جدول‌ها** با pagination، جستجو سمت سرور، مرتب‌سازی ستون‌ها.
- **فرم‌ها** با اعتبارسنجی سمت کاربر و سرور؛ پیام خطای واضح.

### ۱۱) فرایندهای کلیدی (Flow) نمونه
- **تایید انتقال آیتم**:
  1) مدیر وارد بخش Transfers → Pending می‌شود.
  2) «بررسی جزئیات»: مالک، مقصد، نام آیتم، تاریخ.
  3) تایید: جابجایی `user_items` (اگر آیتم در مقصد موجود است، افزایش `quantity`؛ در غیر این صورت ایجاد ردیف جدید). ثبت در `audit_logs`.
  4) اطلاع‌رسانی به هر دو کاربر در تلگرام.
- **اختصاص کشور VIP**:
  1) پروفایل کاربر → بخش کشور → «اختصاص کشور».
  2) لیست کشورها (فیلتر VIP+آزاد) → انتخاب → تایید.
  3) ذخیره در `users.country_id` و ارسال پیام به کاربر.
- **تغییر امتیاز دستی**:
  1) پروفایل کاربر → «تنظیم امتیاز» (افزایش/کاهش با توضیح).
  2) ثبت تراکنش در `transactions` و پیام اطلاع‌رسانی اختیاری به کاربر.

### ۱۲) اجرا، استقرار و عملیات
- **محیط**: Nginx/Apache + PHP-FPM. فعال‌سازی HTTPS اجباری.
- **نسخه‌سازی**: انتشار‌های قابل بازگشت (Rollback) و تهیه Backup از دیتابیس قبل از ارتقا.
- **Cronها**:
  - ارسال سوال: هر ۲ ساعت `php bot.php cron:ask`.
  - پاکسازی سوال‌های منقضی: هر ۵ دقیقه `php bot.php cron:cleanup`.
- **نظارت**: health-check ساده، مانیتورینگ خطاهای PHP (error_log)، هشدار ایمیلی/تلگرامی برای Exceptionهای بحرانی.

### ۱۳) انطباق با جداول موجود
- این پنل به‌طور مستقیم با جداول موجود تعامل می‌کند:
  - کاربران: `users`
  - کشورها: `countries`
  - انواع کارخانه و مالکیت: `factory_types`, `user_factories`
  - فروشگاه و آیتم‌ها: `shop_categories`, `items`, `user_items`, `transactions`
  - ارسالی‌ها: `submissions`
  - انتقال‌ها: `transfers`
  - رویدادها: `events`
  - بسته‌های امتیاز: `point_packages`, درخواست‌های VIP: `vip_purchase_requests`
  - سوالات گروهی: `questions`, `question_posts`
  - تنظیمات: `settings`

در صورت نیاز به ممیزی و ورود مدیر مستقل، پیشنهاد می‌شود جداول زیر نیز افزوده شوند (اختیاری): `admin_users`, `admin_sessions`, `audit_logs`.

### ۱۴) چک‌لیست تست پذیرش
- ایجاد/ویرایش/حذف کشور، دسته‌بندی، آیتم، نوع کارخانه، سوال.
- اختصاص کشور VIP و پس‌گیری.
- خرید آیتم عادی با کسر امتیاز و اعطای دارایی/امتیاز.
- ثبت درخواست خرید VIP و نمایش متن آماده.
- ثبت ارسالی Play، تایید/رد و پیام به کاربر.
- ارتقای کارخانه با محاسبه هزینه و زمان، دریافت درآمد و سردشدن ۲۴ ساعته.
- سوال گروهی: ارسال، پاسخ صحیح کاربر دارای کشور، اعطای جایزه، حذف پیام، پاکسازی خودکار.
- Broadcast به گروه‌های هدف و گزارش وضعیت.
